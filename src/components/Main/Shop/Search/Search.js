import React, {Component} from 'react';
import Navigator from '../../../routes/routeSearch';

class Search extends Component{
    render(){
        return(
            <Navigator />
        );
    }
}
export default Search;