import React, { Component } from 'react';
import Navigator from '../../../routes/routeHome';

class Home extends Component {
    render() {
        return (
            <Navigator />
        );
    }
}
export default Home;